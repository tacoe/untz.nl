
  # Convert to website

  This is a code bundle for Convert to website. The original project is available at https://www.figma.com/design/BKO85rIC6ikFbchrhF4K8g/Convert-to-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  